{
  "name": "ldatext",
  "version": "1.0.0",
  "engines": {
    "node": ">=14.0.0",
    "python": "3.11"
  },
  "scripts": {
    "build": "pip install setuptools wheel && pip install -r requirements.txt"
  }
}
